
export function FilterAlbums ({tableElements, setTableElements, textInInputFilter}) {
    // console.log(tableElements)
    const filteredElements = tableElements.map(item=> item).filter( item => {
        console.log(item)
    })
}